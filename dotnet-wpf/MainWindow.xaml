<Window x:Class="WebCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Web Capture - Presenter" Height="780" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="8" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Button Name="StartBtn" Width="120" Height="34" Click="StartBtn_Click">Start Capture</Button>
            <Button Name="StopBtn" Width="120" Height="34" Click="StopBtn_Click" IsEnabled="False">Stop</Button>
            <Button Name="CopyLinkBtn" Width="160" Height="34" Click="CopyLinkBtn_Click">Copy Share Link</Button>
            <TextBlock Name="StatusText" VerticalAlignment="Center" Margin="12,0,0,0">Ready</TextBlock>
        </StackPanel>

        <wv2:WebView2 Name="WebView" Grid.Row="1" Margin="8" />
    </Grid>
</Window>
